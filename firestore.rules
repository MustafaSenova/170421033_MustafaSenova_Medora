rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geliştirme için basit kurallar - Giriş yapmış tüm kullanıcılar her şeyi okuyabilir/yazabilir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 